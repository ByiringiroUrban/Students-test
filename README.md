# Student API (Node.js + MongoDB)

## Setup
1. Install dependencies:
   npm install

2. Run MongoDB locally

3. Start server:
   npm start

## API Endpoints
- POST /students
- GET /students
- GET /students/:id
- PUT /students/:id
- DELETE /students/:id
